# 🔧 Исправление отображения моделей для изображений

## 📋 Проблема

В Админ-панели в разделе "AI Провайдеры" → "ИИ для изображений" отображаются неверные модели (текстовые модели вместо моделей генерации изображений).

## 🔍 Причина

Некоторые **текстовые** модели имеют в базе данных `capabilities.image = true`, хотя должны иметь `capabilities.image = false`.

**Важно понимать:**
- `capabilities.image = true` означает **ГЕНЕРАЦИЮ изображений** (DALL-E, Imagen, Flux)
- `capabilities.image = false` означает **ТОЛЬКО ТЕКСТ** (GPT, Claude, Gemini text models)
- Все современные модели GPT/Claude/Gemini могут **анализировать** изображения (multimodal), но НЕ могут их **генерировать**

## ✅ Решение

### Шаг 1: Проверить текущее состояние БД

Выполните этот SQL-запрос в **Supabase SQL Editor**:

```sql
-- Показать текстовые модели с неправильным флагом image = true
SELECT
  provider_type,
  model_id,
  model_name,
  capabilities,
  pricing
FROM ai_models
WHERE capabilities->>'text' = 'true'
  AND capabilities->>'image' = 'true'
ORDER BY provider_type, model_id;
```

**Ожидаемый результат:** Должно быть **0 строк** (пусто)

Если видите какие-то модели - переходите к Шагу 2.

### Шаг 2: Применить миграцию исправления

Выполните миграцию в **Supabase SQL Editor**:

```sql
-- Исправить все текстовые модели с неправильным флагом image
UPDATE ai_models
SET capabilities = jsonb_set(capabilities, '{image}', 'false'::jsonb)
WHERE capabilities->>'text' = 'true'
  AND capabilities->>'image' = 'true';
```

Или выполните файл целиком: [migrations/20250131_fix_text_models_image_capability.sql](./migrations/20250131_fix_text_models_image_capability.sql)

### Шаг 3: Проверить модели для изображений

Выполните этот SQL-запрос чтобы убедиться, что модели генерации изображений на месте:

```sql
-- Показать все модели с capabilities.image = true
-- Это должны быть ТОЛЬКО модели генерации изображений
SELECT
  provider_type,
  model_id,
  model_name,
  capabilities,
  pricing->>'output' as price_per_image,
  pricing->>'currency' as currency
FROM ai_models
WHERE capabilities->>'image' = 'true'
  AND is_available = true
ORDER BY provider_type, CAST(pricing->>'output' AS NUMERIC);
```

**Ожидаемый результат (13 моделей):**

#### OpenAI (2 модели):
- dall-e-3 (40.00 USD)
- dall-e-3-hd (80.00 USD)

#### AiTunnel (7 моделей):
- dall-e-2 (3.24 RUB) ⭐ самая дешёвая
- seedream-4-0-250828 (5.40 RUB)
- flux.2-pro (5.40 RUB)
- qwen-image-edit (8.10 RUB)
- dall-e-3 (10.80 RUB)
- flux.2-flex (10.80 RUB)
- gpt-image-1 (11.82 RUB)

#### NeuroAPI (3 модели):
- gemini-2.5-flash-image (0.15 USD) ⭐ самая дешёвая
- gemini-3-pro-image-preview (0.47 USD)
- gpt-image-1 (34.28 USD)

#### Google (1 модель):
- gemini-2.0-flash-exp (бесплатная генерация)

### Шаг 4: Проверить в UI

1. Откройте **Админ-панель** → **AI Провайдеры**
2. Раскройте блок **"ИИ для изображений"**
3. Нажмите "Настроить / Активировать" на любом провайдере
4. В списке моделей должны быть **ТОЛЬКО** модели генерации изображений (см. список выше)

## 🗄️ Если модели для изображений отсутствуют

Если после исправления в списке нет моделей для изображений, выполните миграцию:

```bash
# В Supabase SQL Editor выполните файл:
supabase/migrations/20250129_seed_image_models.sql
```

Эта миграция добавит все модели генерации изображений для всех провайдеров.

## 📚 Документация

- **Структура БД:** [DATABASE_STRUCTURE.md](./DATABASE_STRUCTURE.md) - строки 284-320
- **Миграции:** [supabase/migrations/](./migrations/)
- **Компонент UI:** [components/AIProviders.tsx](../components/AIProviders.tsx) - строка 276

## ❓ Частые вопросы

**Q: Почему GPT-4o не отображается в списке для изображений?**
A: GPT-4o - это **текстовая** модель с поддержкой анализа изображений (multimodal), но она **НЕ может генерировать изображения**. Для генерации нужен DALL-E.

**Q: Почему Claude не может генерировать изображения?**
A: Anthropic Claude **не поддерживает генерацию изображений**. Только анализ. Для генерации используйте OpenAI (DALL-E), Google (Imagen), или прокси (AiTunnel/NeuroAPI).

**Q: Какую модель выбрать для генерации?**
A:
- **Самая дешёвая (RUB):** dall-e-2 через AiTunnel (3.24₽)
- **Самая дешёвая (USD):** gemini-2.5-flash-image через NeuroAPI ($0.15)
- **Лучшее качество:** dall-e-3-hd через OpenAI или flux.2-pro через AiTunnel
- **Бесплатная:** gemini-2.0-flash-exp через Google (в бета-тестировании)

---

**Дата создания:** 2025-01-31
**Автор:** Claude (AI Assistant)
