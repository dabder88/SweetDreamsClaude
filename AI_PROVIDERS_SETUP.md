# 🎯 Инструкция по настройке AI Провайдеров

## ✅ Что было реализовано

Система управления AI провайдерами для PsyDream успешно реализована:

### 1. **База данных**
- ✅ Таблица `ai_provider_configs` - конфигурации провайдеров
- ✅ Таблица `ai_models` - доступные модели AI
- ✅ TOP-20 моделей из AiTunnel и NeuroAPI

### 2. **Backend архитектура**
- ✅ **Adapter Pattern** - универсальные адаптеры для AI провайдеров
- ✅ **BaseProvider** - базовый абстрактный класс
- ✅ **GeminiProvider** - для Google Gemini
- ✅ **OpenAIProvider** - для OpenAI, AiTunnel, NeuroAPI (один адаптер для трех провайдеров!)
- ✅ **ClaudeProvider** - для Anthropic Claude
- ✅ **AIProviderFactory** - фабрика для создания провайдеров
- ✅ **AIService** - главный сервис (singleton) с кэшированием

### 3. **Admin Panel**
- ✅ UI компонент `AIProviders.tsx` для управления провайдерами
- ✅ Выбор активного провайдера
- ✅ Настройка модели AI
- ✅ Тестирование подключения
- ✅ Отображение цен и характеристик моделей

### 4. **Интеграция**
- ✅ Новый раздел в админ-панели "AI Провайдеры"
- ✅ Функции управления в `adminService.ts`

---

## 🚀 Шаги по запуску

### Шаг 1: Запустите SQL миграции в Supabase

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Перейдите в ваш проект → **SQL Editor**
3. Выполните миграции **по порядку**:

#### 1.1. Создание таблиц
```sql
-- Скопируйте и выполните содержимое файла:
-- supabase/migrations/20250129_create_ai_providers.sql
```

#### 1.2. Добавление данных (TOP-20 моделей)
```sql
-- Скопируйте и выполните содержимое файла:
-- supabase/migrations/20250129_seed_ai_providers.sql
```

### Шаг 2: Настройте переменные окружения

Добавьте API ключи в файл `.env`:

```env
# Существующие ключи
VITE_API_KEY=your_gemini_api_key  # Google Gemini (текущий активный)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Новые ключи для AI провайдеров
VITE_AITUNNEL_KEY=your_aitunnel_api_key    # https://aitunnel.ru
VITE_NEUROAPI_KEY=your_neuroapi_api_key    # https://neuroapi.host
VITE_OPENAI_KEY=your_openai_api_key        # https://platform.openai.com
VITE_CLAUDE_KEY=your_claude_api_key        # https://console.anthropic.com
```

### Шаг 3: Получите API ключи

#### AiTunnel (рекомендуется для России)
1. Перейдите на https://aitunnel.ru
2. Зарегистрируйтесь
3. Получите API ключ в личном кабинете
4. **Цены в рублях**: от ₽0.9 за 1M токенов (gpt-5-nano)

#### NeuroAPI (международный, USD)
1. Перейдите на https://neuroapi.host
2. Зарегистрируйтесь
3. Получите API ключ
4. **Цены в долларах**: от $0.04 за 1M токенов

#### OpenAI Direct (опционально)
1. Перейдите на https://platform.openai.com
2. Создайте API ключ

#### Claude (опционально)
1. Перейдите на https://console.anthropic.com
2. Создайте API ключ

### Шаг 4: Запустите приложение

```bash
npm run dev
```

### Шаг 5: Настройте провайдера в админ-панели

1. Войдите как администратор
2. Перейдите в **Админ-панель** → **AI Провайдеры**
3. Выберите провайдера (например, AiTunnel)
4. Нажмите **"Настроить / Активировать"**
5. Выберите модель (например, **gpt-5-mini** - оптимальная по цене/качеству)
6. Нажмите **"Тестировать подключение"** для проверки
7. Нажмите **"Сохранить и активировать"**

---

## 📊 Рекомендуемые модели

### Для России (AiTunnel, рубли)
- **gpt-5-nano** (₽0.9/₽72) - ультра дешевая, для простых задач
- **gpt-5-mini** (₽4.5/₽360) - **ОПТИМАЛЬНАЯ** по цене/качеству ⭐
- **gpt-5** (₽22.5/₽1800) - премиум качество
- **claude-sonnet-4.5** (₽540/₽2700) - максимальное качество

### Для международных пользователей (NeuroAPI, USD)
- **gpt-5-nano** ($0.04/$0.35) - ультра дешевая
- **gpt-5-mini** ($0.22/$1.73) - **ОПТИМАЛЬНАЯ** ⭐
- **gemini-2.5-flash** ($0.24/$2.00) - быстрая и дешевая
- **deepseek-r1** ($0.55/$2.19) - лучшая reasoning модель

---

## 🎯 Как это работает

### Архитектура

```
User Request
    ↓
aiService (Singleton)
    ↓
AIProviderFactory
    ↓
OpenAIProvider / GeminiProvider / ClaudeProvider
    ↓
External AI API (AiTunnel / NeuroAPI / Gemini / Claude)
```

### Ключевая особенность

**ONE OpenAIProvider для ТРЕХ провайдеров!**

AiTunnel, NeuroAPI и OpenAI Direct используют OpenAI-совместимый API, поэтому для всех трех используется один и тот же адаптер `OpenAIProvider`. Единственное отличие - `baseURL`:
- OpenAI: `https://api.openai.com/v1`
- AiTunnel: `https://api.aitunnel.ru/v1`
- NeuroAPI: `https://neuroapi.host/v1`

---

## 🔍 Проверка работоспособности

1. **Тестирование подключения**
   - В админ-панели → AI Провайдеры → Настроить провайдера
   - Нажмите "Тестировать подключение"
   - Должно появиться сообщение "Подключение работает корректно"

2. **Анализ сна**
   - Перейдите на главную страницу
   - Создайте тестовый анализ сна
   - Убедитесь, что анализ проходит успешно

3. **Проверка логов**
   - Откройте Console в браузере (F12)
   - Должны быть логи: `[AIService] Using cached provider: AiTunnel`

---

## ❗ Важные примечания

### Безопасность API ключей
- **MVP подход**: API ключи хранятся в `.env` (клиентская сторона)
- **Для production**: Рекомендуется мигрировать на Supabase Edge Functions + Vault
- **Текущий подход**: Приемлем для development и небольших проектов

### Переключение провайдеров
- Активным может быть только ОДИН провайдер
- При активации нового провайдера, старый автоматически деактивируется
- Конфигурация кэшируется на 1 минуту для оптимизации

### Добавление новых моделей
Через админ-панель или напрямую через SQL:
```sql
INSERT INTO ai_models (provider_type, model_id, model_name, pricing, ...)
VALUES ('aitunnel', 'new-model', 'New Model', '{"input": 1.0, "output": 5.0, ...}', ...);
```

---

## 🐛 Troubleshooting

### Ошибка: "No active AI provider"
- Проверьте, что хотя бы один провайдер активен в админ-панели

### Ошибка: "API key not found"
- Проверьте, что переменная окружения установлена в `.env`
- Перезапустите dev сервер (`npm run dev`)

### Ошибка 401 (Unauthorized)
- Проверьте правильность API ключа
- Убедитесь, что на счету есть средства (для платных провайдеров)

### Ошибка 429 (Rate Limit)
- Слишком много запросов
- Подождите или выберите другого провайдера

---

## 📝 Следующие шаги (опционально)

1. **Миграция на Edge Functions** для безопасного хранения API ключей
2. **Автоматическое переключение провайдеров** при недоступности
3. **A/B тестирование** разных моделей
4. **Мониторинг затрат** и автоматические лимиты
5. **Кэширование ответов** для экономии токенов

---

## ✅ Checklist

- [ ] SQL миграции выполнены в Supabase
- [ ] API ключи добавлены в `.env`
- [ ] Приложение запущено (`npm run dev`)
- [ ] Провайдер выбран и активирован в админ-панели
- [ ] Тестирование подключения пройдено успешно
- [ ] Анализ сна работает корректно

---

Система готова к использованию! 🎉
