# Инструкция по настройке админ-панели

## Критически важная информация

После глубокого анализа были обнаружены и исправлены следующие проблемы:

### ✅ Исправленные проблемы в коде:

1. **adjustBalance** - добавлена универсальная функция для управления балансом
2. **getUserTransactions** - добавлена функция для получения транзакций пользователя
3. **Экспорты типов** - реэкспортированы TransactionType, TransactionStatus, Transaction
4. **getAllUsers** - переписана с использованием RPC вместо прямого доступа к auth.users
5. **getUserDetails** - переписана с использованием RPC вместо прямого доступа к auth.users

### 🔐 Критическое исправление безопасности

**Проблема:** Код пытался напрямую читать таблицу `auth.users` используя анонимный ключ (VITE_SUPABASE_ANON_KEY), что невозможно в Supabase по соображениям безопасности.

**Решение:** Созданы PostgreSQL RPC функции с проверкой прав администратора:
- `get_all_users()` - получение всех пользователей (только для админов)
- `get_user_by_id(UUID)` - получение конкретного пользователя (только для админов)

Эти функции используют `SECURITY DEFINER` для безопасного доступа к `auth.users` и проверяют статус админа перед возвратом данных.

## Шаги по настройке

### 1. Выполните SQL скрипт в Supabase

Откройте [Supabase SQL Editor](https://app.supabase.com) и выполните содержимое файла:

```
SUPABASE_ADMIN_FIX.sql
```

Этот скрипт включает:

✅ **INSERT политику для admin_users** (позволяет автопродвижение в админы)

✅ **Исправленные RLS политики для user_balances:**
- SELECT - пользователи видят свой баланс, админы видят все
- INSERT - пользователи и админы могут создавать записи
- UPDATE - пользователи обновляют свой, админы обновляют все
- DELETE - только админы

✅ **RPC функции для работы с пользователями:**
- `get_all_users()` - список всех пользователей
- `get_user_by_id(UUID)` - данные конкретного пользователя

### 2. Настройте переменные окружения

В файле `.env` должна быть настроена переменная:

```env
VITE_ADMIN_EMAILS=your_admin_email@example.com,another_admin@example.com
```

**Важно:** Несколько email разделяются запятыми без пробелов.

### 3. Проверьте работу

1. Перезапустите dev сервер: `npm run dev`
2. Войдите под email из `VITE_ADMIN_EMAILS`
3. Проверьте что кнопка "Админ-панель" появилась в сайдбаре
4. Откройте админ-панель и перейдите в "Пользователи"
5. Должен загрузиться список пользователей

## Возможные проблемы и решения

### Проблема: "Access denied: admin role required"

**Причина:** Пользователь не добавлен в таблицу `admin_users`

**Решение:**
1. Проверьте что email указан в `VITE_ADMIN_EMAILS`
2. Выйдите из аккаунта и войдите снова
3. Проверьте таблицу `admin_users` в Supabase - должна быть запись с вашим user_id

### Проблема: Пользователи не загружаются

**Причина:** RPC функции не созданы или нет прав

**Решение:**
1. Убедитесь что выполнили `SUPABASE_ADMIN_FIX.sql`
2. Проверьте в Supabase Database -> Functions что функции `get_all_users` и `get_user_by_id` существуют
3. Проверьте логи в консоли браузера (F12)

### Проблема: 406 ошибка при загрузке балансов

**Причина:** RLS политики для `user_balances` не обновлены

**Решение:**
1. Выполните SQL из `SUPABASE_ADMIN_FIX.sql` (секция с user_balances policies)
2. Проверьте что старые политики удалены и созданы новые

## Архитектура решения

### Безопасность

```
Browser (anon key) → RPC Function → Admin Check → auth.users → Response
                          ↓
                    SECURITY DEFINER
                    (server-side execution)
```

**Почему безопасно:**
1. Анонимный ключ остается в браузере (безопасно)
2. Проверка прав происходит на сервере (в PostgreSQL функции)
3. SECURITY DEFINER дает функции временные права доступа
4. RLS политики контролируют кто может вызывать функции

### Структура данных

**admin_users** - таблица с админами
```sql
user_id UUID (PK) → auth.users.id
role TEXT (always 'admin')
created_at TIMESTAMPTZ
```

**user_balances** - балансы пользователей
```sql
user_id UUID (PK) → auth.users.id
balance DECIMAL(10,2)
currency TEXT
```

**transactions** - история транзакций
```sql
id UUID (PK)
user_id UUID → auth.users.id
type TEXT (ADMIN_CREDIT, ADMIN_DEBIT, etc)
amount DECIMAL(10,2)
description TEXT
```

## Что работает сейчас

✅ Админ-панель с защитой по ролям
✅ Список всех пользователей с поиском и фильтрами
✅ Карточка пользователя с детальной информацией
✅ Управление балансом (пополнение/списание)
✅ История транзакций пользователя
✅ Автопродвижение в админы через VITE_ADMIN_EMAILS
✅ Безопасный доступ к данным пользователей через RPC

## Следующие шаги разработки

После проверки что всё работает, можно переходить к:

1. **Финансовый раздел** - полная история транзакций, статистика
2. **Аналитика** - дашборды, графики, метрики
3. **Журнал действий** - аудит лог всех действий админов
4. **Управление AI провайдерами** - настройка Gemini API

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте логи Supabase в дашборде
3. Убедитесь что все SQL скрипты выполнены
4. Проверьте переменные окружения

**Дата создания:** 26 ноября 2025
**Версия:** 1.0
